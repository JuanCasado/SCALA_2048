import java.util
import java.util.Scanner
import java.io.BufferedReader
import java.io.InputStreamReader

object inicio {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(273); 
  
  def programa():Unit = {
				val reader = new Scanner(System.in)
				val reader2 = new Scanner(System.in)
				println(reader)
				programa
				
		};System.out.println("""programa: ()Unit""");$skip(41); 
		
		val reader = new Scanner(System.in);System.out.println("""reader  : java.util.Scanner = """ + $show(reader ))}
		 
}
